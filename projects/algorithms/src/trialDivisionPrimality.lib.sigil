λcandidateDivisors(n:Int)=>[Int]=§numeric.range(
  2,
  n-1
)

λtrialDivisionPrimality(n:Int)=>Bool match n≤1{
  true=>false|
  false=>(candidateDivisors(n) filter (λ(divisor:Int)=>Bool=n%divisor=0) reduce (λ(acc:Int,divisor:Int)=>Int=acc+1) from 0:Int)=0
}
