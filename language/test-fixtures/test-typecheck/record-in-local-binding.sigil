⟦ Test: Record literal with empty list after local binding ⟧

t Inner={items:[ℤ]}
t Outer={state:Inner}

λmain()→𝕌=()

λtest(x:𝔹)→Outer≡x{
  true →
    l dummy=(1:ℤ);
    {state:{items:[]}}|
  false → {state:{items:[1,2,3]}}
}
