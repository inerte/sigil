⟦
  Sigil Standard Library - URL

  Canonical URL parsing for absolute and relative links.
  Runtime behavior is provided by compiler intrinsics for stdlib⋅url.
⟧

t Url={
  fragment:𝕊,
  host:𝕊,
  path:𝕊,
  port:Option[ℤ],
  protocol:𝕊,
  query:{𝕊↦𝕊},
  queryString:𝕊
}

t UrlError={message:𝕊}

i core⋅map

λgetQuery(key:𝕊,url:Url)→Option[𝕊]=core⋅map.get(key,url.query)

λhasQuery(key:𝕊,url:Url)→𝔹=core⋅map.has(key,url.query)

λisAbsolute(url:Url)→𝔹=#url.protocol>0

λisAnchor(url:Url)→𝔹=(#url.path=0) and (#url.fragment>0)

λparse(input:𝕊)→Result[Url,UrlError]=Err({message:"url parse intrinsic unavailable"})

λsuffix(url:Url)→𝕊=url.queryString++url.fragment
