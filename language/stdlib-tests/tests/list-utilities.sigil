i stdlib⋅list
i stdlib⋅numeric

λappend_digit(acc:ℤ,x:ℤ)→ℤ=acc*10+x

λmain()→𝕌=()

test "list.count counts duplicates" {
  stdlib⋅list.count(3,[3,1,3,2,3])=3 and stdlib⋅list.count(9,[])=0 and stdlib⋅list.count(7,[1,2,3])=0
}

test "list.drop clamps naturally" {
  stdlib⋅list.drop(0,[1,2,3])=[1,2,3] and stdlib⋅list.drop(2,[1,2,3,4])=[3,4] and stdlib⋅list.drop(10,[1,2])=[] and stdlib⋅list.drop(-1,[1,2])=[1,2]
}

test "list.find returns first matching option" {
  stdlib⋅list.find(stdlib⋅numeric.is_even,[1,3,4,6])=stdlib⋅list.IntSome(4) and stdlib⋅list.find(stdlib⋅numeric.is_even,[1,3,5])=stdlib⋅list.IntNone()
}

test "list.fold reduces left to right" {
  stdlib⋅list.fold(0,append_digit,[1,2,3])=123 and stdlib⋅list.fold(9,append_digit,[])=9
}

test "list.last returns option" {
  stdlib⋅list.last([])=stdlib⋅list.IntNone() and stdlib⋅list.last([9])=stdlib⋅list.IntSome(9) and stdlib⋅list.last([1,2,3])=stdlib⋅list.IntSome(3)
}

test "list.max returns option" {
  stdlib⋅list.max([])=stdlib⋅list.IntNone() and stdlib⋅list.max([3,9,4])=stdlib⋅list.IntSome(9) and stdlib⋅list.max([-5,-2,-9])=stdlib⋅list.IntSome(-2)
}

test "list.min returns option" {
  stdlib⋅list.min([])=stdlib⋅list.IntNone() and stdlib⋅list.min([3,9,4])=stdlib⋅list.IntSome(3) and stdlib⋅list.min([-5,-2,-9])=stdlib⋅list.IntSome(-9)
}

test "list.nth returns option" {
  stdlib⋅list.nth(0,[7,8])=stdlib⋅list.IntSome(7) and stdlib⋅list.nth(1,[7,8])=stdlib⋅list.IntSome(8) and stdlib⋅list.nth(2,[7,8])=stdlib⋅list.IntNone() and stdlib⋅list.nth(-1,[7,8])=stdlib⋅list.IntNone()
}

test "list.product multiplies integer lists" {
  stdlib⋅list.product([])=1 and stdlib⋅list.product([2,3,4])=24 and stdlib⋅list.product([5,-2,1])=-10
}

test "list.remove_first removes only first match" {
  stdlib⋅list.remove_first(2,[1,2,3,2])=[1,3,2] and stdlib⋅list.remove_first(9,[1,2,3])=[1,2,3] and stdlib⋅list.remove_first(9,[])=[]
}

test "list.reverse reverses list" {
  stdlib⋅list.reverse([])=[] and stdlib⋅list.reverse([1])=[1] and stdlib⋅list.reverse([1,2,3])=[3,2,1]
}

test "list.sum sums integer lists" {
  stdlib⋅list.sum([])=0 and stdlib⋅list.sum([1,2,3,4])=10 and stdlib⋅list.sum([5,-2,1])=4
}

test "list.take clamps naturally" {
  stdlib⋅list.take(0,[1,2,3])=[] and stdlib⋅list.take(2,[1,2,3,4])=[1,2] and stdlib⋅list.take(10,[1,2])=[1,2] and stdlib⋅list.take(-1,[1,2])=[]
}
