⟦
  Sigil Standard Library - Time

  Canonical instant model (epoch millis) with strict ISO-8601 parsing.
  Runtime behavior is provided by compiler intrinsics for stdlib⋅time.
⟧

t Instant={epochMillis:ℤ}
t TimeError={message:𝕊}

λcompare(left:Instant,right:Instant)→ℤ=0

λformatIso(instant:Instant)→𝕊=""

λfromEpochMillis(millis:ℤ)→Instant={epochMillis:millis}

λisAfter(left:Instant,right:Instant)→𝔹=false

λisBefore(left:Instant,right:Instant)→𝔹=false

λnow()→!IO Instant={epochMillis:0}

λparseIso(input:𝕊)→Result[Instant,TimeError]=Err({message:"time parse intrinsic unavailable"})

λtoEpochMillis(instant:Instant)→ℤ=instant.epochMillis
