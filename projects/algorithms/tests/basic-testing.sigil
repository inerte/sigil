i stdlib⋅numeric

λmain()→𝕌=()

mockable λping()→!IO 𝕊="real"

test "mockable function can be overridden" →!IO {
  with_mock(ping, λ()→!IO 𝕊="fake") {
    ping()="fake"
  }
}

test "nested with_mock uses inner replacement lexically" →!IO {
  with_mock(ping, λ()→!IO 𝕊="outer") {
    with_mock(ping, λ()→!IO 𝕊="inner") {
      ping()="inner"
    }
  }
}

test "numeric predicates are available in projects" {
  stdlib⋅numeric.is_even(2)=true∧stdlib⋅numeric.is_odd(3)=true
}

test "pure boolean passes" {
  1+1=2
}

test "with_mock can be used in boolean expressions" →!IO {
  with_mock(ping, λ()→!IO 𝕊="pong") {
    ping()="pong"∧ping()≠"real"
  }
}
