t Todo={done:𝔹,id:ℤ,text:𝕊}

i src⋅todo-domain
i stdlib⋅numeric

λlenTodos(todos:[Todo])→ℤ match todos{
  []→0|
  [_,.rest]→1+lenTodos(rest)
}

λmain()→𝕌=()

test "todo add prepends item" {
  match src⋅todo-domain.addTodo(1,"Task",[]){
    [todo]→todo.id=1 and todo.text="Task" and todo.done=false|
    _→false
  }
}

test "todo clearCompleted keeps active only" {
  match src⋅todo-domain.clearCompleted([Todo{done:true,id:1,text:"A"},Todo{done:false,id:2,text:"B"}]){
    [todo]→todo.id=2 and todo.done=false|
    _→false
  }
}

test "todo completedCount counts completed" {
  src⋅todo-domain.completedCount([Todo{done:true,id:1,text:"A"},Todo{done:false,id:2,text:"B"},Todo{done:true,id:3,text:"C"}])=2
}

test "todo completedCount result can use numeric predicates" {
  l count=(src⋅todo-domain.completedCount([Todo{done:true,id:1,text:"A"},Todo{done:false,id:2,text:"B"},Todo{done:true,id:3,text:"C"}]):ℤ);
  stdlib⋅numeric.is_even(count)=true and stdlib⋅numeric.in_range(3,0,count)=true
}

test "todo delete reduces length" {
  lenTodos(src⋅todo-domain.deleteTodo(1,[Todo{done:false,id:1,text:"A"},Todo{done:false,id:2,text:"B"}]))=1
}

test "todo delete removes target" {
  match src⋅todo-domain.deleteTodo(1,[Todo{done:false,id:1,text:"A"},Todo{done:false,id:2,text:"B"}]){
    [todo]→todo.id=2 and todo.text="B"|
    _→false
  }
}

test "todo edit updates text" {
  src⋅todo-domain.editTodo("New",1,[Todo{done:false,id:1,text:"Old"}])[0].text="New"
}

test "todo toggle flips done flag" {
  src⋅todo-domain.toggleTodo(1,[Todo{done:false,id:1,text:"Task"}])[0].done=true
}
