i stdlib⋅numeric

λmain()→𝕌=()

test "numeric.in_range is inclusive" {
  stdlib⋅numeric.in_range(3,1,3)=true∧stdlib⋅numeric.in_range(4,1,3)=false
}

test "numeric.is_even and is_odd basics" {
  stdlib⋅numeric.is_even(4)=true∧stdlib⋅numeric.is_odd(5)=true
}

test "numeric.is_positive and is_negative basics" {
  stdlib⋅numeric.is_positive(5)=true∧stdlib⋅numeric.is_negative(-1)=true
}
