# AGENTS.md

Open `README.md` before changing this project.
Keep `src/main.sigil` as the only public runner entrypoint.
Preserve stable check ids, issue codes, and JSON output shape when extending the audit surface.
