---
description: Audit the current Sigil PR branch with the shared sigil-pr-audit skill before reading the implementation diff.
---

Load and follow the `sigil-pr-audit` skill from `.claude/skills/sigil-pr-audit`.

Audit the current checked-out branch first. Use any optional arguments as `[base-ref] [scope]`.

Arguments: $ARGUMENTS
