λmain()=>!IO [ConcurrentOutcome[Int,String]]=concurrent urlAudit({concurrency:1,jitterMs:None(),stopOn:stopOn,windowMs:None()}){
  spawnEach [1,2,3] process
}

λprocess(value:Int)=>!IO Result[Int,String]=Ok(value)

λstopOn(err:String)=>Bool=false
