i src⋅graph-helpers
i src⋅graph-types
i stdlib⋅list

λbfs_loop(graph:src⋅graph-types.Graph,queue:[ℤ],visited:[ℤ])→[ℤ] match queue{
  []→visited|
  [head,.tail]→match stdlib⋅list.contains(head,visited){
    true→bfs_loop(graph,tail,visited)|
    false→{
      l next_queue=(tail⧺src⋅graph-helpers.neighbors_of(graph,head):[ℤ]);
      l next_visited=(visited⧺[head]:[ℤ]);
      bfs_loop(graph,next_queue,next_visited)
    }
  }
}

λbreadth_first_search(graph:src⋅graph-types.Graph,start:ℤ)→[ℤ] match src⋅graph-helpers.has_vertex(graph,start){
  true→bfs_loop(graph,[start],[])|
  false→[]
}
