⟦ String Predicates

   Boolean validation predicates for string properties.
   Part of Sigil standard library - canonical implementations only.

   These functions are compiler intrinsics - they have type signatures
   but no bodies. The compiler emits optimized JavaScript directly.
⟧

⟦ Check if string starts with prefix - compiler intrinsic ⟧
export λstarts_with(s:𝕊,prefix:𝕊)→𝔹=⊥

⟦ Check if string ends with suffix - compiler intrinsic ⟧
export λends_with(s:𝕊,suffix:𝕊)→𝔹=⊥
