i src⋅graph-helpers
i src⋅graph-types
i stdlib⋅list

λdepth_first_search(graph:src⋅graph-types.Graph,start:ℤ)→[ℤ] match src⋅graph-helpers.has_vertex(graph,start){
  true→dfs_visit(graph,start,[])|
  false→[]
}

λdfs_neighbors(graph:src⋅graph-types.Graph,neighbors:[ℤ],visited:[ℤ])→[ℤ] match neighbors{
  []→visited|
  [neighbor,.tail]→{
    l next_visited=(dfs_visit(graph,neighbor,visited):[ℤ]);
    dfs_neighbors(graph,tail,next_visited)
  }
}

λdfs_visit(graph:src⋅graph-types.Graph,vertex:ℤ,visited:[ℤ])→[ℤ] match stdlib⋅list.contains(vertex,visited){
  true→visited|
  false→{
    l next_visited=(visited⧺[vertex]:[ℤ]);
    dfs_neighbors(graph,src⋅graph-helpers.neighbors_of(graph,vertex),next_visited)
  }
}
