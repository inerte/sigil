⟦
  Sigil Standard Library - File

  Canonical UTF-8 filesystem helpers.
⟧

e fs

e fs⋅promises

i stdlib⋅path

λappendText(content:𝕊,path:𝕊)→!IO 𝕌=fs⋅promises.appendFile(path,content,"utf8")

λexists(path:𝕊)→!IO 𝔹=fs.existsSync(path)

λlistDir(path:𝕊)→!IO [𝕊]=fs⋅promises.readdir(path)

λmakeDir(path:𝕊)→!IO 𝕌=fs⋅promises.mkdir(path,{recursive:false})

λmakeDirs(path:𝕊)→!IO 𝕌=fs⋅promises.mkdir(path,{recursive:true})

λreadText(path:𝕊)→!IO 𝕊=fs⋅promises.readFile(path,"utf8")

λremove(path:𝕊)→!IO 𝕌=fs⋅promises.unlink(path)

λremoveTree(path:𝕊)→!IO 𝕌=fs⋅promises.rm(path,{force:true,recursive:true})

λwriteText(content:𝕊,path:𝕊)→!IO 𝕌=fs⋅promises.writeFile(path,content,"utf8")
