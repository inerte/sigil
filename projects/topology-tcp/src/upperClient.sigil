i src::topology

i stdlib::tcpClient

λmain()=>!Tcp String match stdlib::tcpClient.send(src::topology.eventStream,"upper:hello"){
  Ok(response)=>response.message|
  Err(error)=>"ERR:"++error.message
}
