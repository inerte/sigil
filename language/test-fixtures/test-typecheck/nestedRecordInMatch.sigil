⟦ Test: Nested record with empty lists in match expression ⟧

t Inner={items:[ℤ]}
t Outer={inner:Inner,name:𝕊}
t Result={data:Outer}

λmain()→𝕌=()

λtest(x:𝔹)→Result match x{
  true → {data:{inner:{items:[]},name:"test"}}|
  false → {data:{inner:{items:[1,2,3]},name:"other"}}
}
