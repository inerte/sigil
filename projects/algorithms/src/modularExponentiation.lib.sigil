λmodPow(base:ℤ,exponent:ℤ,modulus:ℤ)→ℤ match modulus=1{
  true→0|
  false→match exponent=0{
    true→1%modulus|
    false→match exponent%2=0{
      true→{
        l half=(modPow(base,exponent/2,modulus):ℤ);
        (half*half)%modulus
      }|
      false→(((base%modulus)*modPow(base,exponent-1,modulus))%modulus)
    }
  }
}
