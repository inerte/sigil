t Tick=Tick()

e stdlibTimer:{afterMs:λ(Int)=>!Timer Owned[§stream.Source[Unit]],everyMs:λ(Int)=>!Timer Owned[§stream.Source[Unit]]}

λafterMs(ms:Int)=>!Timer Owned[§stream.Source[Unit]]=stdlibTimer.afterMs(ms)

λeveryMs(ms:Int)=>!Timer Owned[§stream.Source[Unit]]=stdlibTimer.everyMs(ms)
