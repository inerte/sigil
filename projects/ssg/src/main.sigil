e process

i src⋅build
i src⋅types

λbase_path()→𝕊=base_path_from_property(process.env.hasOwnProperty("sigil_site_base_path"))

λbase_path_from_property(has_value:𝔹)→𝕊 match has_value{
  true→(process.env.sigil_site_base_path:𝕊)|
  false→"/"
}

λmain()→!IO 𝕌=
  src⋅build.build_site({
    base_path:base_path(),
    output_dir:"website/.local/site",
    repo_url:repo_url(),
    site_description:"Machine-first programming language designed for canonical code generation.",
    site_title:"Sigil"
  })

λrepo_url()→𝕊=repo_url_from_property(process.env.hasOwnProperty("sigil_site_repo_url"))

λrepo_url_from_property(has_value:𝔹)→𝕊 match has_value{
  true→(process.env.sigil_site_repo_url:𝕊)|
  false→""
}
