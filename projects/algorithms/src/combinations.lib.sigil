mode total

λcombinations(k:Int,xs:[Int])=>[[Int]]
requires 0≤k and 0≤#xs
=combinationsInto(
  [],
  k,
  xs
)

λcombinationsInto(acc:[[Int]],k:Int,xs:[Int])=>[[Int]]
requires 0≤k and 0≤#xs
decreases (k, #xs)
match k{
  0=>[[]]⧺acc|
  _=>match xs{
    []=>acc|
    [
  head,
  .tail
]=>(combinationsInto(
      [],
      k-1,
      tail
    ) map (λ(choice:[Int])=>[Int]=[head]⧺choice):[[Int]])⧺combinationsInto(
      acc,
      k,
      tail
    )
  }
}
