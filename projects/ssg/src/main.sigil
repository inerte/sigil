e process

i src⋅build
i src⋅types

λbasePath()→𝕊=basePathFromProperty(process.env.hasOwnProperty("sigilSiteBasePath"))

λbasePathFromProperty(hasValue:𝔹)→𝕊 match hasValue{
  true→(process.env.sigilSiteBasePath:𝕊)|
  false→"/"
}

λmain()→!IO 𝕌=
  src⋅build.buildSite({
    basePath:basePath(),
    outputDir:"website/.local/site",
    repoUrl:repoUrl(),
    siteDescription:"Machine-first programming language designed for canonical code generation.",
    siteTitle:"Sigil"
  })

λrepoUrl()→𝕊=repoUrlFromProperty(process.env.hasOwnProperty("sigilSiteRepoUrl"))

λrepoUrlFromProperty(hasValue:𝔹)→𝕊 match hasValue{
  true→(process.env.sigilSiteRepoUrl:𝕊)|
  false→""
}
