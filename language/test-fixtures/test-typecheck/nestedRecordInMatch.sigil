t Inner={items:[Int]}

t Outer={inner:Inner,name:String}

t Result={data:Outer}

λmain()=>Unit=()

λtest(x:Bool)=>Result match x{
  true=>{data:{inner:{items:[]},name:"test"}}|
  false=>{data:{inner:{items:[1,2,3]},name:"other"}}
}
