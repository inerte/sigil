i src::httpHelpers

i src::topology

i stdlib::httpClient

i stdlib::string

λmain()=>!Http String match stdlib::httpClient.get(src::topology.mailerApi,stdlib::httpClient.emptyHeaders(),"/routes"){
  Ok(response)=>stdlib::string.intToString(response.status)++":"++response.body|
  Err(error)=>"ERR:"++error.message
}
