e console

e process

λdebug(msg:String)=>!IO Unit=console.debug(msg)

λeprintln(msg:String)=>!IO Unit=console.error(msg)

λprint(msg:String)=>!IO Unit=process.stdout.write(msg)

λprintln(msg:String)=>!IO Unit=console.log(msg)

λwarn(msg:String)=>!IO Unit=console.warn(msg)
