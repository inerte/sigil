⟦ Typed FFI calls also compose under the concurrent-by-default model. ⟧

t AssetPair={config:𝕊,template:𝕊}

e fs⋅promises : {
  readFile : λ(𝕊, 𝕊) → 𝕊
}

λload_config()→𝕊=fs⋅promises.readFile("config.json","utf8")

λload_template()→𝕊=fs⋅promises.readFile("template.html","utf8")

λmain()→AssetPair={
  l config=load_config();
  l template=load_template();
  {config:config,template:template}
}
