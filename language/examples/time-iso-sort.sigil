i stdlib⋅time

λmain()→𝕌 match stdlib⋅time.parse_iso("2026-03-01"){
    Ok(first)→
      match stdlib⋅time.parse_iso("2026-03-03"){
        Ok(second)→
          l _=(stdlib⋅time.compare(first,second):ℤ);
          ()|
        Err(_)→()
      }|
    Err(_)→()
  }
