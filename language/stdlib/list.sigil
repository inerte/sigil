⟦ List Standard Library

   Canonical list utilities and predicates.
   Part of Sigil standard library - canonical implementations only.

   Note: Use # operator for length instead of a function (e.g., #[1,2,3] → 3)
⟧

⟦ Helper: Check if index is in bounds ⟧
λin_bounds_helper(idx:ℤ,xs:[ℤ])→𝔹≡(idx≥0){
  ⊤→idx<#xs|
  ⊥→⊥
}

⟦ Helper: Check ascending order ⟧
λsorted_asc_check(prev:ℤ,xs:[ℤ])→𝔹≡xs{
  []→⊤|
  [b,.rest]→≡(prev≤b){
    ⊤→sorted_asc_check(b,rest)|
    ⊥→⊥
  }
}

⟦ Helper: Check descending order ⟧
λsorted_desc_check(prev:ℤ,xs:[ℤ])→𝔹≡xs{
  []→⊤|
  [b,.rest]→≡(prev≥b){
    ⊤→sorted_desc_check(b,rest)|
    ⊥→⊥
  }
}

⟦ Check if all elements satisfy predicate ⟧
export λall(pred:λ(ℤ)→𝔹,xs:[ℤ])→𝔹≡xs{
  []→⊤|
  [x,.rest]→≡pred(x){
    ⊤→all(pred,rest)|
    ⊥→⊥
  }
}

⟦ Check if any element satisfies predicate ⟧
export λany(pred:λ(ℤ)→𝔹,xs:[ℤ])→𝔹≡xs{
  []→⊥|
  [x,.rest]→≡pred(x){
    ⊤→⊤|
    ⊥→any(pred,rest)
  }
}

⟦ Check if element is in list ⟧
export λcontains(item:ℤ,xs:[ℤ])→𝔹≡xs{
  []→⊥|
  [x,.rest]→≡(x=item){
    ⊤→⊤|
    ⊥→contains(item,rest)
  }
}

⟦ Get first element of list (unsafe - crashes on empty) ⟧
export λhead(xs:[ℤ])→ℤ≡xs{
  [x,.rest]→x
}

⟦ Check if index is valid for list [0, len-1] ⟧
export λin_bounds(idx:ℤ,xs:[ℤ])→𝔹=in_bounds_helper(idx,xs)

⟦ Check if list is empty ⟧
export λis_empty(xs:[ℤ])→𝔹≡xs{
  []→⊤|
  _→⊥
}

⟦ Check if list is non-empty ⟧
export λis_non_empty(xs:[ℤ])→𝔹≡xs{
  []→⊥|
  _→⊤
}

⟦ Check if list is sorted in ascending order ⟧
export λsorted_asc(xs:[ℤ])→𝔹≡xs{
  []→⊤|
  [_]→⊤|
  [a,.tail]→sorted_asc_check(a,tail)
}

⟦ Check if list is sorted in descending order ⟧
export λsorted_desc(xs:[ℤ])→𝔹≡xs{
  []→⊤|
  [_]→⊤|
  [a,.tail]→sorted_desc_check(a,tail)
}

⟦ Get all elements except first (unsafe - crashes on empty) ⟧
export λtail(xs:[ℤ])→[ℤ]≡xs{
  [x,.rest]→rest
}
