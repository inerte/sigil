e console

e process

λdebug(msg:String)=>!Log Unit=console.debug(msg)

λeprintln(msg:String)=>!Log Unit=console.error(msg)

λprint(msg:String)=>!Log Unit=process.stdout.write(msg)

λprintln(msg:String)=>!Log Unit=console.log(msg)

λwarn(msg:String)=>!Log Unit=console.warn(msg)
