i core⋅option
i core⋅result

λmain()→𝕌=()

test "core prelude exposes option constructors without import" {
  match Some(42){
    Some(value)→value=42|
    None()→false
  }
}

test "core prelude exposes result constructors without import" {
  match Ok("done"){
    Ok(value)→value="done"|
    Err(error)→false
  }
}

test "core option helpers stay namespaced" {
  core⋅option.is_some(Some(1)) and core⋅option.is_none(None()) and core⋅option.unwrap_or(7,None())=7
}

test "core result helpers stay namespaced" {
  core⋅result.is_ok(Ok(1)) and core⋅result.is_err(Err("bad")) and core⋅result.unwrap_or_result(9,Err("bad"))=9
}
