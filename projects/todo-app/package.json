{
  "name": "@sigil-lang/example-todo-react",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sigil:compile": "cargo run -q -p sigil-cli --no-default-features -- compile src/todoDomain.lib.sigil -o web/src/generated/todo-domain.ts",
    "predev": "pnpm sigil:compile",
    "dev": "vite",
    "prebuild": "pnpm sigil:compile",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "@vitejs/plugin-react": "^5.0.0"
  }
}
