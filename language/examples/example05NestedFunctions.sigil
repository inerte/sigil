⟦ Test: Nested Function Calls
   Expected output: 50
   Tests: function composition
⟧

λadd(a:Int,b:Int)=>Int=a+b

λcompute()=>Int=add(multiply(10,3),multiply(5,4))

λmain()=>Int=compute()

λmultiply(a:Int,b:Int)=>Int=a*b
